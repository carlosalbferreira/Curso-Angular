console.log([1, 2, 3, 4, 5,"Carlos", true].length);

// o array o primiro elemento sempre é zero e assim por diante. 

["Carlos", "Alberto"].forEach((res, index) => {
    console.log(res, index);
});

