let arr = [1, 2, 3, 4, 5]; 

console.table(arr);

console.log("Shift", arr.shift()); //Remove o primeiro elemento do array
console.log("Pop", arr.pop()); //Remove o ultimo elemento do array

console.table(arr);